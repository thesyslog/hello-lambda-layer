# ObjetId user fields
OID_USER_FIELDS = ["site_id", "ent_id", "_id"]

# boundaries are based on the user used for queries
BOUNDARIES = ["site_id"]

# Define waht fields are mandatories in each collection
MANDATORY_FIELDS = ["site_id", "ent_id"]

# Headers for success and error responses 
# HEADERS = {
#                'Access-Control-Allow-Origin': "*",
#                'Access-Control-Allow-Methods': "*"
#           }
HEADERS = None

AUTHORIZER = "custom"